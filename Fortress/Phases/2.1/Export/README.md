# Export — Phase 2.1

**Status:** Stub — full `Project/` mirror and zip pending shootout schedule.

**Source of truth:** `Fortress/Phases/2.1/Project/` (living agent package).

When ready:

1. Copy `Fortress/Phases/2.1/Project/` → `Fortress/Phases/2.1/Export/Project/` (full tree)
2. Add or refresh `Export/Prompts/Build-Prompt.md`
3. Zip per `Fortress/PROCESS.md` naming convention; place zip here

**Shootout instruction:** copy `Export/Project/` to an external build root. Agent project root = folder containing that phase's `AGENTS.md`.