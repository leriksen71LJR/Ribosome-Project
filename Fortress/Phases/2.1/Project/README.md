# Phase 2.1 — Project

**Specification Pipeline** prototype (Exp06 fork + tensions/REGISTRY split).

All Phase 2.1 prep lives here. **Agent entry:** `AGENTS.md`.

| Path | Purpose |
|------|---------|
| `AGENTS.md` | Project root — highest authority for shootouts |
| `.documents/` | Authoritative build docs (read-only for agents) |
| `.codingAgent/` | Agent proposals (writable, non-authoritative) |
| `Builds/` | Combined build reports |
| `Decisions/`, `Plans/`, `Guidance/` | Phase prep feeding the agent package |

**Phase root** (`../`): preflight and research handoff (`Phase-2.1-*.md`) — steward/director, not agent path.

**Export** (`../Export/`): mirror + prompts + zip when running a shootout.